<div class="activities-container">
  <div class="user-container">
    <div class="user-card">
      <div class="image-container">
        <% if current_user.image.attached? %>
          <%= image_tag current_user.image,width:'60'  %>
        <% else %>
          <%= image_tag 'avatar.webp',width:'60'  %>
        <% end %>
      </div>
      <p>Report for</p>
      <h2>
        <%= current_user.getName  %>
        <br/>
        <%= current_user.lastName %>
      </h2>
    </div>
    <div class="timeframes" >
      <% Activity::TIMEFRAMES.each do |timeframe|  %>
        <%= button_to timeframe.capitalize(), edit_activity_path(timeframe:timeframe, id:1) %>
      <% end %>
    </div>
  </div>
  <div id="turbo_activities">
    <%= render partial: 'activities/activity', locals: { timeframe: 'week'} %>
  </div>
</div>
